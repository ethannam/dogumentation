<div class="ui container" style="margin-top: 0.5em;">
  <div class="ui large secondary menu">
    <div id="logo" class="header item">
      <a href="/" style="color: black;">🐾 &nbsp;Dogumentation</a>
    </div>
    <div class="right item">
      <%= link_to "Console", user_path(@user.username), class: "active item" %>
      <%= link_to "Edit Profile", edit_user_path(@user.id), class: "item" %>
      <%= form_tag logout_path, method: :delete, id: "form_logout" do %>
      <% end %>
      <button type="submit" form="form_logout", value="Submit" class="ui button item">Log Out</button>
    </div>
  </div>
</div>